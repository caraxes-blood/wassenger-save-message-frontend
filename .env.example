# Required for production builds / hosted deploys. Local `next dev` falls back to this URL if unset.
NEXT_PUBLIC_WASSENGER_API_BASE_URL=https://wassenger-save-webhook-message-production.up.railway.app
